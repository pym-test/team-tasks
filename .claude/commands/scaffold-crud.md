---
description: 새 CRUD API와 UI를 한 번에 만든다
---

다음 리소스에 대한 풀스택 CRUD를 만들어 주십시오.

리소스 이름: $ARGUMENTS

요구사항:
- DB 테이블 생성과 마이그레이션
- API Routes (GET, POST, PATCH, DELETE)
- UI 페이지 (목록, 폼, 삭제)
- RLS 정책 (auth.uid() 기반)

기존 패턴은 src/lib과 app/api/tasks를 참고.
